# T3.19 — Evidência (SPEC-3-019) — v0.0.570 QA verde

## Implementado

- Migration 0185: coleção relatorios_agendados (nome, tipo, periodicidade semanal|mensal, dia_semana, hora_utc, destinatarios, ativo, criado_por, ultimo_envio_em, ultimo_status; regras auth; delete null) + auditoria.acao += relatorio_enviado.
- Migration 0186 (fix): campo ativo sem required — BoolField required rejeitava false como blank ("ativo: cannot be blank" ao desativar).
- Hook relatorios_agendados.js: GET/POST/PATCH /relatorios + POST /{id}/enviar (envio manual) + cron horário "15 \* \* \* \*" idempotente (semanal: dia_semana+hora_utc; mensal: dia 1+hora_utc; não reenvia na mesma hora). Gerador de HTML inline nos dois callbacks (AP-0200 — o gate de integrações rejeitou a função top-level e o arquivo foi corrigido).
- UI: página /relatorios (AdminRoute) com lista, criar, ativar/desativar, "Enviar agora" + rota no App.tsx + card "Relatórios agendados" na home (admin).
- LGPD: e-mail contém só KPIs agregados + link do painel — nenhum dado pessoal de contato.

## Provas por API (base real)

RED 9: 401 GET/POST/enviar sem auth; 400 e-mail inválido; 400 periodicidade inválida; 404 enviar inexistente; 403 operator POST/PATCH/enviar.
GREEN: POST cria (t319_prova, semanal domingo 10:00 UTC, deniane@vibratto.com.br); GET lista; PATCH ativo false/true 200 (após fix 0186); **envio manual 200 — 1 e-mail enviado, 0 falhas** (log da plataforma: "email sent", tipo transactional, assunto "CRM Vibratto — Resumo da Direção (2026-09-01 a 2026-09-13)"); ultimo_envio_em + ultimo_status=enviado gravados; auditoria relatorio_enviado (enviados:1, falhas:0, destinatarios:1).
Regressão: painel 200, metas 200, obrigações 200, visão coordenação 200, contratos 200, ficha operacional 200 (rota correta /ficha-operacional/{empresaId} com o id da EMPRESA zat97l1mnjwclqi — os 404 iniciais foram id errado do teste, não falha).
Limpeza: agendamento de prova t319_prova DESATIVADO (ativo=false) e preservado na base para o teste humano da CEO.

## Bugs corrigidos no caminho

1. Migration 0185 ausente do working tree (POST dava "sql: no rows in result set") — arquivo recriado (v0.0.569).
2. AP-0200 reincidente: função top-level rejeitada pelo gate de integrações — inline duplicado no POST enviar e no cron (v0.0.568).
3. BoolField required rejeita false como blank no JSVM — fix migration 0186 (v0.0.570).

## Observações

- O cron real dispara a "15 \* \* \* \*" (min 15 past, piso de 10 min da plataforma); a prova do disparo agendado completo acontece no primeiro horário agendado real (a lógica é a mesma do envio manual, provado).
