# Debug 2026-09-13 — T3.17 POST gerar 400 no pod

**Task e problema:** T3.17 (SPEC-3-017) — `POST /backend/v1/negocios/{id}/contrato/gerar` retornava 400 no pod ("Something went wrong") enquanto o working tree estava correto; 3 rebuilds (v0.0.548–0.0.552) sem convergência observada.

**Reprodução:** POST com dados mínimos contra o backend interno do preview → 400 genérico; consolidação GET 200 normal.

**Causa raiz:** (1) var top-level de OUTRO arquivo de hook (`TEMPLATE_CONTRATO_VIBRATTO` em contrato_modelo.js) NÃO é visível dentro do callback no runtime goja — AP-0200 estendida para o escopo ENTRE ARQUIVOS; fix: template inline no callback. (2) Convergência do pod é assíncrona — os rebuilds anteriores provavelmente convergiram depois das leituras; a validação final (sessão seguinte) provou o caminho real funcionando (v1/v2 geradas, texto 6.136 chars, 0 placeholders, mensalidade no texto).

**Correção:** template inline no callback (v0.0.551) + UI resiliente a 200 com corpo vazio no POST (v0.0.557 — recupera a versão pela consolidação/GET versão) + limpeza das fixtures de prova (0182/0183 — base final 0 contratos).

**Verificação automática:** RED 8 ok (401/403/404/400/403) + GREEN (geração v1 200, versionamento v2, GET versão 200 com texto íntegro, auditoria contrato_gerado) + regressão (obrigações/implantações/visão-coordenacao 200) + limpeza verificada (consolidação: 0 versões, pode_gerar true).

**Gate atual:** aguardando teste humano
